/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.coinranking.com', 'www.bing.com', 'ih1.redbubble.net'],
  },
}

module.exports = nextConfig
